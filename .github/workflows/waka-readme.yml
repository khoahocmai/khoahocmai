name: Waka Readme

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # Runs at 12am UTC
    - cron: "0 0 * * *"

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
          # ✅ Hiển thị thông tin coding
          SHOW_UPDATED_DATE: true
          SHOW_TOTAL_CODE_TIME: true
          SHOW_LANGUAGE: true
          SHOW_TIME: true
          SHOW_PERCENTAGE: true
          
          # 🚫 Ẩn các thứ không cần thiết
          SHOW_LINES_OF_CODE: false
          SHOW_COMMIT: false
          SHOW_PROJECTS: false
          SHOW_EDITORS: false
          SHOW_OPERATING_SYSTEM: false
          SHOW_PROFILE_VIEWS: false
          SHOW_SHORT_INFO: false
          SHOW_LOC_CHART: false
          SHOW_LANGUAGE_PER_REPO: false

          # ✨ Tuỳ chỉnh khác
          CUSTOM_WORKSPACE_PATH: ./
          SHOW_TITLE: true

          # ✅ Commit lại README
          COMMIT_MESSAGE: Update WakaTime coding metrics
